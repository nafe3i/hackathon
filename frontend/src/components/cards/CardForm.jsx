export default function CardForm({ form, setForm, editing, error, onSubmit, onCancel }) {
  return <section className="panel"><h2>{editing ? "Modifier la carte" : "Nouvelle carte"}</h2><form onSubmit={onSubmit}><label>Mon état<input required value={form.state} onChange={(e) => setForm({ ...form, state: e.target.value })} placeholder="Ex. Besoin de calme" /></label><label>Mon message<textarea required rows="4" value={form.text} onChange={(e) => setForm({ ...form, text: e.target.value })} placeholder="Ex. J’ai besoin d’un endroit silencieux." /></label><label>Ton<select value={form.tone} onChange={(e) => setForm({ ...form, tone: e.target.value })}><option value="neutre">Neutre</option><option value="chaleureux">Chaleureux</option><option value="formel">Formel</option></select></label><label className="check"><input type="checkbox" checked={form.is_public} onChange={(e) => setForm({ ...form, is_public: e.target.checked })}/> Visible sur mon profil public</label><label className="check"><input type="checkbox" checked={form.is_shared} onChange={(e) => setForm({ ...form, is_shared: e.target.checked })}/> Partager avec mon réseau</label>{error && <p className="error">{error}</p>}<div className="form-actions"><button>{editing ? "Enregistrer" : "Créer la carte"}</button>{editing && <button type="button" className="ghost" onClick={onCancel}>Annuler</button>}</div></form></section>;
}
